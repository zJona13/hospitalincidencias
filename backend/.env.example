PORT=3000

DB_HOST=localhost
DB_USER=sql_db_user
DB_PASS=sql_db_password
DB_NAME=hospital_incidencias

JWT_SECRET=supersecreto

PUBLIC_URL=http://localhost:3000
PUBLIC_URL_FRONT=http://localhost:8080

# Correo saliente para recuperación de contraseña.
# Si SMTP_HOST y SMTP_USER quedan vacíos, el enlace se imprime en la consola del backend.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
